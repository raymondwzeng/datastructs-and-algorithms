package Algorithms;

public class Quicksort {
    public static void Sort(int[] arr)
    {
        int left = 0;
        int right = arr.length - 1;
        int pivot = (right-left)/2; //This will be our pivot. There is likely a better method, but we'll just use this for now.
        
    }
}
